---
description: PlasmoのContent Scripts UI機能を使って既存のウェブページに独自のコンポーネントを挿入する方法。
---

import { Callout } from "nextra-theme-docs"

# Content Scripts UI

Plasmoは、React、Svelte3、またはVue3コンポーネントを現在のウェブページにマウントするためのファーストクラスのサポートを提供します。この機能は、コンテンツスクリプトUI（CSUI (Content Scripts UI)）と呼ばれます。

拡張機能は必要に応じていくつでもCSUIを持つことができ、各CSUIは、[configオブジェクトをエクスポートする](/framework/content-scripts#config)ことで、ウェブページのグループまたは特定のウェブページをターゲットとします。Reactを使用してUIの挿入を開始するには：

1. `content.tsx`を作成する
1. デフォルトのReactコンポーネントをエクスポートする
1. 利益 🎉

```tsx filename="content.tsx"
const CustomButton = () => {
  return <button>Custom button</button>
}

export default CustomButton
```

<Callout emoji="👀" title="Note">
  **NOTE**: 複数のコンテンツスクリプトを持つには、`contents/`ディレクトリを作成し、そこにCSUIエントリを配置します。
</Callout>

完全な例については、[with-content-scripts-ui](https://github.com/PlasmoHQ/examples/tree/main/with-content-scripts-ui)を参照してください。

### Config

Content Scripts UIはコンテンツスクリプトのサブセットです。そのため、通常のPlasmoコンテンツスクリプトと同様に、[configオブジェクトをエクスポート](/framework/content-scripts#config)することができます。

## Plasmo CSUIの動作原理

**TL;DR**: Plasmoは[Shadow DOM](https://developer.mozilla.org/en-US/docs/Web/Web_Components/Using_shadow_DOM)を作成し、エクスポートされたコンポーネントをそこにマウントします。この分離テクニックにより、ウェブページのスタイルがコンポーネントのスタイルに影響を与えること、およびその逆を防止します。

詳細については、[Plasmo CSUIのライフサイクル](./content-scripts-ui/life-cycle)を参照してください。


