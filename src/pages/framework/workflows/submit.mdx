---
description: ブラウザープラットフォーム公開GitHubアクションを使用して、拡張機能をさまざまなウェブストアに自動的にデプロイする方法について説明します。
---

import { Callout } from "nextra-theme-docs"

# 拡張機能の提出

Plasmo Frameworkには、[Browser Platform Publish](https://github.com/marketplace/actions/browser-platform-publisher/)（BPP）と呼ばれる便利なGitHubアクションが付属しています。このアクションは、サポートされているすべてのブラウザ拡張機能ストアに拡張機能を自動的に公開します。デフォルトでは手動トリガーで実行されますが、設定を変更することで、プッシュごとに実行するように設定できます。

Plasmo拡張機能の公開を開始するには、次のスキーマを使用して`keys.json`ファイルを設定します。

```json
{
  "$schema": "https://raw.githubusercontent.com/PlasmoHQ/bpp/v3/keys.schema.json"
}
```

このスキーマは、エディタが[`JSON schema`](https://json-schema.org/)をサポートしている場合に役立ちます。有効な提出資格情報のみを宣言してください。そうでない場合、アクションは失敗します。

<Callout emoji="🙆‍♀️">
  注記: 親愛なる近所の`.gitignore`のおかげで、このファイルはgitによって無視されます
</Callout>

提出に必要なトークンについて詳しくは、[トークンガイド](https://github.com/PlasmoHQ/bms/blob/main/tokens.md)をご覧ください。

最終的なキーは次のようになります。

```json
{
  "$schema": "https://raw.githubusercontent.com/plasmo-corp/bpp/v3/keys.schema.json",
  "chrome": {
    "clientId": "123",
    "refreshToken": "789",
    "extId": "abcd",
    "clientSecret": "efgh"
  },
  "edge": {
    "clientId": "aaaaaaa-aaaa-bbbb-cccc-dddddddddddd",
    "clientSecret": "abcdefg",
    "productId": "aaaaaaa-aaaa-bbbb-cccc-dddddddddddd",
    "accessTokenUrl": "https://login.microsoftonline.com/aaaaaaa-aaaa-bbbb-cccc-dddddddddddd/oauth2/v2.0/token"
  }
}
```

このキーをコピーし、`SUBMIT_KEYS`という名前で[GitHubのリポジトリシークレット](https://docs.github.com/en/actions/security-guides/encrypted-secrets#creating-encrypted-secrets-for-a-repository)として追加します。

その後、GitHubで新しい拡張機能バージョンを提出する必要があるときに、手動でアクションをトリガーします！


